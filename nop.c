#include "monty.h"

/**
  *f_nop- A function for nothing in stack
  *@headr: the stack's headr
  *@counters: the line_number contents
  *Return: no return
 */

void f_nop(stack_t **headr, unsigned int counters)
{
	(void) counters;
	(void) headr;
}
